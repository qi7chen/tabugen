// This file is auto-generated by Tabular v0.8.1, DO NOT EDIT!

#include "GuideDefine.h"
#include <stddef.h>
#include <assert.h>
#include <memory>
#include <fstream>
#include "common/Conv.h"

using namespace std;

#ifndef ASSERT
#define ASSERT assert
#endif


namespace config {

// parse data object from an csv row
int NewbieGuideDefine::ParseFromRow(const std::vector<absl::string_view>& row, NewbieGuideDefine* ptr)
{
    ASSERT(row.size() >= 6);
    ASSERT(ptr != nullptr);
    ptr->Name = to<std::string>(row[0]);
    ptr->Type = to<std::string>(row[1]);
    ptr->Target = to<std::string>(row[2]);
    {
        const std::vector<absl::string_view>& array = absl::StrSplit(row[3], ",");
        for (size_t i = 0; i < array.size(); i++)
        {
            ptr->Accomplishment.push_back(to<int16_t>(array[i]));
        }
    }
    {
        const std::vector<absl::string_view>& vec = absl::StrSplit(row[4], ";");
        for (size_t i = 0; i < vec.size(); i++)
        {
            const std::vector<absl::string_view>& kv = absl::StrSplit(vec[i], "=");
            ASSERT(kv.size() == 2);
            if(kv.size() == 2)
            {
                const auto& key = to<std::string>(kv[0]);
                ASSERT(ptr->Goods.count(key) == 0);
                ptr->Goods[key] = to<uint32_t>(kv[1]);
            }
        }
    }
    ptr->Description = to<std::string>(row[5]);
    return 0;
}


} // namespace config 
